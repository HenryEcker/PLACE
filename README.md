# PLACE
Git
Force pull:
git reset --hard
git fetch --all
git pull

Git Add:
git -add .

Git Commit:
git commit -a
git commit -am

Coding:
To generate UniqueID in React components
_.uniqueId('contact_');
// => 'contact_104'

_.uniqueId();
// => '105'
