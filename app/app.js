import React from 'react';
import ReactDOM from 'react-dom';
import PMApp from './components/pmApp';

//var separatedInteractions = getseparatedInteractions();

ReactDOM.render(
    <PMApp userID={1}/>, document.getElementById('MainPanel'));
